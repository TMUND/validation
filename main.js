(function() {
  // ----------
  window.App = {
    // ----------
    init: function() {
      var self = this;

    },

    // ----------
    _makeApiCall: function(tag) {
    }
  };

  // ----------
  $(document).ready(function() {
    App.init();
  });

})();
